# 北大法宝 MCP 多密钥配置（复制为 auth.env 后编辑，auth.env 已被 .gitignore 排除）
#
# 用途：当一个密钥失效(401/403)、限速(429)或暂时无法访问时，自动切换到下一个。
# 优先级：环境变量 PKULAW_MCP_AUTHORIZATION > 本文件（自上而下） > ~/.pkulaw/mcp/config.json
#
# 写法：每行一个密钥，编号从 1 开始递增；值可写完整头 'Bearer xxx'，也可只写裸 token（自动补 Bearer）。
# 按可用性排序：把最稳定/额度最足的密钥放最前面。
#
# ⚠️ 下面 KEY1/KEY2 均为示例占位符，不是真实密钥，请替换为你自己的 Token。

KEY1='Bearer YOUR_TOKEN_HERE_00000000-0000-0000-0000-000000000000'
KEY2='YOUR_BACKUP_TOKEN_HERE_00000000-0000-0000-0000-000000000000'
